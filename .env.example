# Database
DATABASE_URL="postgresql://user:password@localhost:5432/tute_valenciano"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3000
NODE_ENV="development"
FRONTEND_URL="http://localhost:5173"

# Email (SMTP) — Opcional, necesario para recuperación de contraseña
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="Tute Parrillano <your-email@gmail.com>"

# ── Producción (Railway) ──
# NODE_ENV="production"
# DATABASE_URL → Lo proporciona Railway automáticamente
# FRONTEND_URL="https://tu-app.railway.app"
# JWT_SECRET → Cambiar a una cadena aleatoria segura
